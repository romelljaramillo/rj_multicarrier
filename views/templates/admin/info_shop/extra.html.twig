{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
  <div class="card">
    <div class="card-header d-flex align-items-center justify-content-between">
      <h3 class="card-title mb-0">{{ 'Configuración adicional'|trans({}, 'Modules.RjMulticarrier.Admin') }}</h3>
      <a class="btn btn-outline-secondary btn-sm" href="{{ path('admin_rj_multicarrier_info_shop_index') }}">{{ 'Volver'|trans({}, 'Admin.Actions') }}</a>
    </div>
    <div class="card-body">
      {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_row(form.RJ_ETIQUETA_TRANSP_PREFIX) }}
        {{ form_row(form.RJ_MODULE_CONTRAREEMBOLSO) }}

        <div class="mt-3">
          <button type="submit" class="btn btn-primary">{{ 'Guardar'|trans({}, 'Admin.Actions') }}</button>
        </div>
      {{ form_end(form) }}
    </div>
  </div>
{% endblock %}
